intérprete de comando para introducir expresiones matematicas
	- permitir asiagnaciones a variables
	-	reasignaciones
	-	distinguir si con punto y coma o no el resultado se saca por pantalla o no
	- permitir manejar operaciones aritmeticas basicas
	- parentesis para precedencia
	- criterios de precedencia claros
	- utilizar signo menos como operador binario o unitario
	- introducir funciones matemáticas, prioritariamente funciones de un argumento o más de  un argumento
	- seno de unha matriz é o seno de todos os elementos da matrjz
	- non se fai declarion de tipos
	- todas as operacions son sobre número reales 1+0.5 = 1.5
	- se puede trabajar con constantes pi, e ,etc

	- como gestionar expresiones
			- como interprete de comandos
			- ejecutando un fichero que contiene una secuencia de expresiones
				-utilizar fichero como argumento al invocar la consola
				-durate la ejecución de la linea de comandos permitir utilizar un comando que realice la carga de un fichero.(Todo o contexto queda como esta e se lle añade ao contexto o contenido do archivo)

	- gestion de variables mediante a tabla de símbolos
			-lexema,componenteLexico,valor

	- comando para permitir imprimir tabla de simbolos con identificadores e valores actuales

	- gestión de errores:
		- utilizar variable que no ha sido inicializada
		- reasignaciones a una variable
		- parentesis mal anidados
		- operadores usados incorrectamente
		- divisiones entre 0

	- pode cambiar o valor a unha variable as veces que se queira
	- ayuda que se le proporciona al usuario

	- gestión de funciones de librería

- adjuntar fichero para que compile flex
- adjuntar fichero para que compile bson
