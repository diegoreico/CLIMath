intérprete de comando para introducir expresiones matematicas
	x permitir asiagnaciones a variables
	x	reasignaciones
	x	distinguir si con punto y coma o no el resultado se saca por pantalla o no
	x permitir manejar operaciones aritmeticas basicas
	x parentesis para precedencia
	x criterios de precedencia claros
	x utilizar signo menos como operador binario o unitario
	+/- introducir funciones matemáticas, prioritariamente funciones de un argumento o más de  un argumento
	- seno de unha matriz é o seno de todos os elementos da matrjz
	x non se fai declarion de tipos
	x todas as operacions son sobre número reales 1+0.5 = 1.5
	- se puede trabajar con constantes pi, e ,etc

	- como gestionar expresiones
			x como interprete de comandos
			x ejecutando un fichero que contiene una secuencia de expresiones
				-utilizar fichero como argumento al invocar la consola
				x durate la ejecución de la linea de comandos permitir utilizar un comando que realice la carga de un fichero.(Todo o contexto queda como esta e se lle añade ao contexto o contenido do archivo)

	- gestion de variables mediante a tabla de símbolos
			-lexema,componenteLexico,valor

	- comando para permitir imprimir tabla de simbolos con identificadores e valores actuales

	- gestión de errores:
		- utilizar variable que no ha sido inicializada
		- reasignaciones a una variable
		- parentesis mal anidados
		- operadores usados incorrectamente
		- divisiones entre 0

	x pode cambiar o valor a unha variable as veces que se queira
	x ayuda que se le proporciona al usuario

	- gestión de funciones de librería

x adjuntar fichero para que compile flex
x adjuntar fichero para que compile bson
